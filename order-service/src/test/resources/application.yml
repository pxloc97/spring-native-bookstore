spring:
  cloud:
    config:
      enabled: false
    function:
      definition: dispatchOrder;handleInventoryDecision
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://localhost:9999/oauth2/jwks

polar:
  catalog-service-url: http://localhost:9001
