---
description: How to define and use agent skills
---

# Skills Configuration

This file defines the custom skills agents can use within the project.

1. Implement new skills in the `.agents/skills` directory as `.py` or `.sh` files.
2. Provide a descriptive `README.md` for each skill.
3. Test skills independently before allowing agents to use them in the wild.
